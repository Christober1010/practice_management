/**
 * @deprecated Use scheduling-flows.spec.ts for session notes + data collection UI.
 */
import { test } from "@playwright/test";

test.describe.skip("Data collection — session notes UI (moved)", () => {
  test("see scheduling-flows.spec.ts", () => {});
});
