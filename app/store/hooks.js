'use client'

import { useDispatch, useSelector } from 'react-redux'

// Simple passthrough hooks for JS projects
export const useAppDispatch = () => useDispatch()
export const useAppSelector = useSelector
